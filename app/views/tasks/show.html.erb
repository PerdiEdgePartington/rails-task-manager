<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<p><%= link_to "update task", "/tasks/#{@task.id}/edit" %><p>
<p><%= link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>

<p><%= link_to "back to list", "/tasks" %><p>
